# Docker Compose 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env

# 호스트 포트 설정 (기본값: 3000)
# 다른 포트를 사용하려면 변경하세요 (예: 8080)
HOST_PORT=3000

# 관리자 계정 설정
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# JWT 시크릿 키 (보안을 위해 반드시 변경하세요!)
# 랜덤 문자열 생성: openssl rand -base64 32
JWT_SECRET=your-random-jwt-secret-key-change-this-in-production

# DDNS 업데이트 주기 (분 단위, 기본값: 5분)
UPDATE_INTERVAL=5