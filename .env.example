# Docker Compose Environment Variables
# Copy this file to .env and update with your values: cp .env.example .env

# ⚠️ SECURITY WARNING: Change ALL default values below!

# Port Configuration
# Change if port 3000 is already in use
HOST_PORT=3000

# Admin Credentials (MUST CHANGE!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme  # ← CHANGE THIS!

# JWT Secret Key (MUST CHANGE IN PRODUCTION!)
# Generate a secure random key with: openssl rand -base64 32
JWT_SECRET=your-random-jwt-secret-key-change-this-in-production  # ← CHANGE THIS!

# DDNS Update Interval (minutes)
# Recommended: 5-30 minutes
UPDATE_INTERVAL=5

# Cookie Configuration for Proxy Setup
# Set to true if using HTTPS (required for secure cookies)
USE_HTTPS=true

# Optional: Set specific cookie domain (only if needed for subdomain sharing)
# COOKIE_DOMAIN=.seongjun.kr

# Site URL Configuration
# Set this for consistent URL generation (optional - auto-detected if not set)
NEXT_PUBLIC_SITE_URL=https://ddns.yourdomain.com
NEXTAUTH_URL=https://ddns.yourdomain.com